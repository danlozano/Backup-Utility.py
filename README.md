Backup-Utility.py
=================

Simple utility for figuring out which files to backup. It takes as input the type of files, size and dates you which to backup and outputs a .txt file with a list of the files that need to be backed up.